using System;

namespace Notes
{
    class Program
    {
        static void Main(string[] args)
        {
            

            // we have For loops
            // Foreach loops
            // While loops
            // Do-While loops


            // initialization clause, this is our counter
            // condition clause, for loop will run as long as this is valid
            // incremention clause. incremenets till 10
            for (var i = 0; i < 10; i++)
            {

            }


            // used to iterate over elements in an ennumerable object
            // anything with a list or array nature
            // strings
            // easier than a four loop
            // local variable number and list or object we are iterating (numbers)
            foreach (var number in numbers)
            {

            }


            // as long as this condition is valid, we will execute
            // typically have ac counter we initalize before loop is executed and at the end we increment
            while (i < 10)
            {
                ...
                i++;
            }


            // executes at least once, and condition is evaluated at the end of the loop
            do
            {
                ...
                i++
            } while (i < 10)



            //  break - jumps out of loop
            //  continue - jumps to the next iteration



            // RANDOM CLASS


            

        }
    }
}
